<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/small_margin"
    android:background="@color/light_grey">

    <ImageView
        android:id="@+id/image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="@dimen/min_trailer_width"
        android:minHeight="@dimen/min_trailer_height"
        android:layout_gravity="center"
        android:adjustViewBounds="true"
        android:contentDescription="@string/trailers_thumbnail_descriptor"
        android:cropToPadding="false"
        android:scaleType="centerInside" />

    <ProgressBar
        android:id="@+id/progress_bar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

    <ImageView
        android:id="@+id/playButton_iv"
        android:layout_width="@dimen/play_button_size"
        android:layout_height="@dimen/play_button_size"
        android:layout_gravity="center"
        android:alpha="0.5"
        android:contentDescription="@string/play_button_descriptor"
        app:srcCompat="@drawable/ic_play_circle_outline_white_24dp" />
</FrameLayout>